name: Comment asobi
on:
  issue_comment:
    types: [created, edited]

jobs:
  asobi:
    if: contains(github.event.comment.html_url, '/pull/') && startsWith(github.event.comment.body, '/asobi')
    permissions:
      pull-requests: write
      contents: write
      checks: read
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Get argument
        id: get_arg
        run: |
          echo "ARGS=`echo ${{ github.event.comment.body }} | awk '{print $2}'`" >> "$GITHUB_OUTPUT"

      - name: Arg trigger
        if: contains(github.event.comment.body, 'dayo')
        uses: ./.github/actions/asobi
        with:
          arg: ${{ steps.get_arg.outputs.ARGS }}

      - name: Refer
        run: |
          echo "branch_ref: ${{ github.ref }}"
